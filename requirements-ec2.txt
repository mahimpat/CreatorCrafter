# EC2-Optimized Requirements for CreatorCrafter
# Install in this order to avoid xformers issues

# IMPORTANT: Install PyTorch FIRST using this command:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

# After PyTorch is installed, run:
# pip install --prefer-binary -r requirements-ec2.txt

# Core dependencies
numpy>=1.24.0,<2.0.0
scipy>=1.11.0
Pillow>=10.0.0

# Video Processing
opencv-python>=4.8.0

# AI/ML Models (torch should already be installed)
transformers>=4.30.0
openai-whisper>=20231117

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0

# Scene Detection
scenedetect[opencv]>=0.6.0

# AudioCraft (install last, will show xformers warnings - ignore them)
audiocraft>=1.0.0
